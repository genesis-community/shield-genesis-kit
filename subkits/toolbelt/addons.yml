instance_groups:
  - name: shield
    jobs:
      - (( append ))
      - { release: toolbelt, name: toolbelt}
      - { release: toolbelt, name: toolbelt-quick}

releases:
- name: toolbelt
  version: latest
