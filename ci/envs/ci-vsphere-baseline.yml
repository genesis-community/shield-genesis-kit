---
kit:
  ci:
    job: deploy
    serial_groups:
    - deployment

genesis:
  env:   ci-vsphere-baseline

params:
  #network: shield
  #vm_type: small
  shield_static_ip: (( concat meta._ip_prefix "8.16" ))
  installation:     S.H.I.E.L.D. CI/CD
